# 湖北智能旅游平台 - 项目文件说明

## 项目概述

本项目是一个基于 Vue 3 + TypeScript + Node.js 的湖北智能旅游平台，包含前端展示系统和管理后台。

---

## 根目录文件

| 文件 | 说明 |
|------|------|
| `index.html` | 项目入口 HTML 文件 |
| `package.json` | 前端依赖配置（Vue、Vite、TypeScript 等） |
| `vite.config.ts` | Vite 构建工具配置 |
| `tsconfig.json` | TypeScript 编译配置 |
| `tailwind.config.js` | Tailwind CSS 样式框架配置 |
| `postcss.config.js` | PostCSS 配置文件 |
| `README.md` | 项目说明文档 |

---

## 后端 (backend/)

### 配置文件

| 文件 | 说明 |
|------|------|
| `server.js` | Express 服务器入口，启动后端服务 |
| `.env` | 环境变量配置（数据库连接、JWT密钥等） |
| `package.json` | 后端依赖配置（Express、MySQL、JWT 等） |
| `init-db.js` | 数据库初始化脚本，创建数据表并插入示例数据 |

### 核心模块

| 文件 | 说明 |
|------|------|
| `config/database.js` | MySQL 数据库连接配置 |
| `middleware/auth.js` | JWT 认证中间件和权限验证 |

### API 路由

| 文件 | 说明 |
|------|------|
| `routes/auth.js` | 用户认证相关 API（注册、登录、修改密码等） |
| `routes/attractions.js` | 景点管理 API（CRUD、热度统计、浏览/收藏/评论计数） |
| `routes/reviews.js` | 评论管理 API（添加评论、获取评论、删除评论） |
| `routes/routes.js` | 旅游路线 API（暂未实现完整功能） |
| `routes/users.js` | 用户信息 API（用户资料相关） |
| `routes/admin.js` | 管理员专用 API（用户管理、热度管理、系统统计） |
| `routes/ai.js` | AI 助手 API（调用 AI 服务进行对话） |

---

## 前端 (src/)

### 入口文件

| 文件 | 说明 |
|------|------|
| `main.ts` | Vue 应用入口文件，挂载根组件 |
| `App.vue` | 根组件，包含路由视图 |
| `style.css` | 全局样式文件 |
| `vite-env.d.ts` | Vite 类型声明文件 |

### 视图页面 (views/)

| 文件 | 说明 |
|------|------|
| `Home.vue` | 首页，展示热门景点和欢迎信息 |
| `Attractions.vue` | 景点列表页，支持搜索、筛选、收藏 |
| `AttractionDetail.vue` | 景点详情页，展示景点信息、评论、收藏等 |
| `Routes.vue` | 旅游路线列表页 |
| `RouteDetail.vue` | 路线详情页 |
| `Favorites.vue` | 用户收藏页面，展示已收藏的景点 |
| `AIChat.vue` | AI 助手页面，智能对话和快捷问题 |
| `Login.vue` | 用户登录页面 |
| `Register.vue` | 用户注册页面 |
| `ForgotPassword.vue` | 忘记密码页面 |
| `Profile.vue` | 用户个人资料页面（编辑资料、修改密码、查看收藏等） |
| `Admin.vue` | 管理员面板（用户管理、景点管理、评论管理、系统统计） |

### 组件 (components/)

| 文件 | 说明 |
|------|------|
| `Navigation.vue` | 顶部导航栏组件 |
| `AIChat.vue` | AI 聊天组件（消息展示、输入框、发送功能） |
| `WeatherWidget.vue` | 天气小组件（展示当前位置天气） |

### 可复用逻辑 (composables/)

| 文件 | 说明 |
|------|------|
| `useAuth.ts` | 认证状态管理（登录、登出、Token 验证、权限判断） |
| `usePopularity.ts` | 景点热度管理（加载景点、热度统计、浏览/收藏/评论计数） |
| `useAdmin.ts` | 管理员功能（用户列表、角色管理、统计数据加载） |
| `useComments.ts` | 评论管理（加载评论、添加评论、删除评论） |
| `useWeather.ts` | 天气数据获取和缓存 |
| `useAIService.ts` | AI 对话服务（调用后端 AI 接口） |

### 服务层 (services/)

| 文件 | 说明 |
|------|------|
| `api.ts` | API 服务层，封装所有后端接口调用（axios 实例、请求拦截器、响应拦截器） |

### 类型定义 (types/)

| 文件 | 说明 |
|------|------|
| `index.ts` | TypeScript 类型定义（User、Attraction、Route、Review 等接口） |

### 静态数据 (data/)

| 文件 | 说明 |
|------|------|
| `attractions.ts` | 静态景点数据（备用数据源） |

### 工具函数 (utils/)

| 文件 | 说明 |
|------|------|
| `initData.ts` | 初始化数据工具（页面加载时初始化必要数据） |

---

## 热度统计规则

```
热度 = 基础热度 + 浏览次数 × 0.1 + 收藏次数 × 2.0 + 评论次数 × 1.0
```

| 行为 | 热度贡献 |
|------|----------|
| 基础热度 | 管理员设置（默认 10） |
| 浏览1次 | + 0.1 |
| 收藏1次 | + 2.0 |
| 评论1次 | + 1.0 |

---

## 数据库表结构

| 表名 | 说明 |
|------|------|
| `users` | 用户信息表 |
| `attractions` | 景点信息表 |
| `reviews` | 评论表 |
| `routes` | 旅游路线表 |
| `popularity_logs` | 热度变更日志表 |

---

## 路由说明

### 前端路由

| 路径 | 页面 |
|------|------|
| `/` | 首页 |
| `/attractions` | 景点列表 |
| `/attractions/:id` | 景点详情 |
| `/routes` | 路线列表 |
| `/routes/:id` | 路线详情 |
| `/favorites` | 我的收藏 |
| `/ai-chat` | AI 助手 |
| `/login` | 登录 |
| `/register` | 注册 |
| `/forgot-password` | 忘记密码 |
| `/profile` | 个人中心 |
| `/admin` | 管理面板（需管理员权限） |

### 后端 API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录 |
| GET | `/api/auth/me` | 获取当前用户信息 |
| GET | `/api/attractions` | 获取景点列表 |
| GET | `/api/attractions/:id` | 获取景点详情 |
| POST | `/api/attractions` | 添加景点（管理员） |
| PUT | `/api/attractions/:id` | 更新景点（管理员） |
| DELETE | `/api/attractions/:id` | 删除景点（管理员） |
| POST | `/api/reviews` | 添加评论 |
| GET | `/api/reviews/all` | 获取所有评论（管理员） |
| GET | `/api/admin/users` | 获取用户列表（管理员） |
| PUT | `/api/admin/users/:id/role` | 修改用户角色（管理员） |
| POST | `/api/ai/chat` | AI 对话 |

---

## 技术栈

### 前端
- **Vue 3** - 渐进式 JavaScript 框架
- **TypeScript** - 类型安全的 JavaScript 超集
- **Vite** - 下一代前端构建工具
- **Vue Router** - Vue.js 官方路由管理器
- **Tailwind CSS** - 实用优先的 CSS 框架
- **Axios** - HTTP 请求库

### 后端
- **Node.js** - JavaScript 运行时
- **Express** - Web 应用框架
- **MySQL** - 关系型数据库
- **JWT** - JSON Web Token 认证
- **Bcrypt** - 密码加密

---

## 运行项目

### 启动后端
```bash
cd backend
npm install
node server.js
```

### 启动前端
```bash
npm install
npm run dev
```

### 初始化数据库
```bash
cd backend
node init-db.js
```
